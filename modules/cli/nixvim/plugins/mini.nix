{
  hm.programs.nixvim.plugins.mini = {
    enable = true;
    # TODO Check out the other modules that mini offers
    modules = {
      ai.n_lines = 500;
      pairs = {};
      statusline = {};
      surround = {};
    };
  };
}
